 long long mul_inv(long long a, long long b);

 long long crt(const long long,
               const long long,
               const long long,
               const long long,
               const long long);
